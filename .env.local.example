# Local secrets — copy to .env.local (gitignored) and fill in values.
# Loaded after .env by start.sh; overrides DEEPSEEK_API_KEY for dev/CLI/tests.
#
#   cp .env.local.example .env.local
#
# Get a key: https://platform.deepseek.com/

DEEPSEEK_API_KEY=
